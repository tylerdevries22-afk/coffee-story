{
  "name": "@platform/schema",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "verify": "pnpm typecheck && pnpm test",
    "seed": "cd ../.. && tsx scripts/onboard.ts --tenant demo-roastery --require-db",
    "migrate": "./migrate.sh"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.111.0"
  }
}
