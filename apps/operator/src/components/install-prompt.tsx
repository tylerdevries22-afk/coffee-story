/**
 * Invitation to install the app to the home screen.
 *
 * Native builds are already installed, so there is nothing to offer here. The
 * web build substitutes `install-prompt.web.tsx`, which is where the real
 * behaviour lives.
 */
export function InstallPrompt() {
  return null;
}
