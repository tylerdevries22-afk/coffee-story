{
  "name": "@platform/ui",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo 'lint: covered by consuming apps until Phase 3'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet'",
    "verify": "pnpm typecheck && pnpm test"
  }
}
