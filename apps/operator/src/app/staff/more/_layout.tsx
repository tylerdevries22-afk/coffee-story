import { Stack } from 'expo-router';

/** Staff's equivalent of `client/more/_layout.tsx` -- see that file. */
export default function StaffMoreLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
