{
  "name": "@platform/links",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo 'lint: runs under each consuming app eslint'",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "verify": "pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "@types/node": "~24.6.2",
    "tsx": "^4.20.6",
    "typescript": "~5.9.2"
  }
}
