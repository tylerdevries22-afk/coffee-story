/** How far below the strip a section has to reach before it counts as current. */
export const SECTION_ACTIVATION_OFFSET = 140;

/** A 48pt pinned category row plus its hairline. */
export const STRIP_HEIGHT = 49;
