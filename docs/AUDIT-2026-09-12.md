# Production and franchise readiness audit — 2026-09-12

Seven parallel read-only audit workers over `dev` at `08bb7264`, one scope each
(money, security/RLS, database, franchise/tenant, app flows, CI/release, test
quality), integrated and re-verified by the coordinator. Every item below
carries a file:line that was actually read and a **CONFIRMED** / **SUSPECTED**
label as the worker reported it. Where the coordinator re-verified a claim
before acting, that is noted; two worker claims were dropped on re-verification
and are not listed.

Severity is calibrated to the platform's actual state: one live brand,
zero production orders, real Square credentials, real database.

Status legend: **DONE** (merged to dev), **PR** (open), **W1** (wave-1 worker
running in an isolated worktree), **QUEUED** (next wave), **OWNER** (needs an
action only the owner can take), **DEFERRED** (not reachable today; stated
trigger for when it is).

---

## 1. Landed this session

| PR | Change |
| --- | --- |
| #136 | Fulfillment capability guard trigger (catering/delivery need an active installation) |
| #137 | One tenant's vocabulary no longer leaks into every tenant |
| #139 | Tax rounds on the decimal value, not the float representing it — `200 × 0.0725` billed 14c, owed 15c; zero divergences 0–100,000c after |
| #140 | Platform fee returns in proportion when an order is refunded |
| #141 | Delivery held off for the coffee-story launch — the guest was quoted a fee the engine cannot charge |
| #142 | Fulfillment trigger maps to a structured error instead of a raw 42501 |
| #143 | Production can no longer fall back to the demo session (`DEMO_SESSION` is a `platform_admin`) |
| #144 | The delivery gate is an assertion, not a convention — proven to fire |
| #128 | Explicit Go live; five inert guards now carry the real flag; `next build` runs for the first time on that branch |
| #147 (PR) | `pnpm -r verify` gets `--no-bail` — the class of failure that hid 27 build errors |
| #148 (PR) | A generic tenant no longer ships a coffee shop's home screen (juniper-base-demo was rendering "Latte Lover" and "Boba Week") |
| #149 (PR) | HQ stops offering the delivery module the guest app cannot serve |

Promoted to `main` twice (#145, #146). `main` = `b97f4a12`.

---

## 2. Wave 1 — thirteen isolated workers, in flight

Migration numbers are pre-assigned so branches cannot collide; the coordinator
bumps `REQUIRED_DATABASE_RELEASE` once at integration.

| Worker | Migration | Scope |
| --- | --- | --- |
| Public RPC wrappers | `20260912030000` | Five RPCs exist only in `app.*`, which PostgREST does not expose (`config.toml:20`) — `revoke_device_installation`, `record_device_heartbeat`, `create_device_stream_session`, `set_brand_kiosk_config`, `publish_catalog_draft` are all dead. **An owner cannot revoke a stolen wall tablet.** Plus a consistency test: every `.rpc('x')` resolves to `public.x`. |
| Refund definer + writer assertion | `20260912040000` | `process_square_refund` was re-created `SECURITY INVOKER` twice after being asserted `DEFINER`, so the registered assertion fails permanently and takes down the readiness gate. Argument-identity-writer guard is a literal list of 8 that misses 4 OAuth writers; becomes pg_proc-driven. |
| Location-scoped RLS | `20260912050000` | Six tables with `location_id` gated on `brand_id` only (`crew_tasks`, `workforce_role_assignments`, `site_module_overrides`, three `connector_*`); `locations_update` admits role `staff`. |
| Platform-admin live membership | `20260912060000` | `app.is_platform_admin()` reads only the JWT; an offboarded admin keeps cross-tenant read across 78 policy sites for a full token lifetime. |
| HQ API hardening | — | `authenticatedDb` built with the service-role key as `apikey`; heartbeat route guard is a tautology; device credential mint/rotate/revoke routes are unthrottled and the limiter keys on attacker-settable headers. |
| Operator error surfacing | — | Refund/cancel failures land in `conflicts`, which no screen reads — a barista sees the sheet close as if done. Board false-empty on cold start. Calendar load failure reads as "nothing scheduled". |
| HQ dead buttons | `20260912090000` if needed | "Schedule a drop" and "Save draft" are `type="button"` with no handler. The flagship rotating-drop feature writes nothing. |
| Customer fake success | — | Catering "Send request" is `setSent(true)` with no backend call. Referral flow promises a credit no function delivers. |
| Money hardening | `20260912070000` | Refunds are not serialized — two devices can both validate $15 against a $20 order. Nothing rejects `fee_bps_tier2 > fee_bps`, which silently charges MORE above the threshold. |
| FK indexes + generic assertion | `20260912080000` | Six cascading FKs with no covering index, three on the live location-delete path; a generic `pg_constraint` assertion so the class cannot recur. |
| CI security | — | Sentry DSN is never wired into `deploy-hosted.yml` — production error tracking is a silent no-op. No SAST or secret scanning exists (CI-01). |
| Payment-path tests | — | `cancelOrder` with a bound payment id; fee-quote release on definitive rejection; fresh-attempt identity mismatch; malformed tenders; the once-a-month `platform-fee-volume` clock race. |
| Schema/state tests | — | The order transition guard is never triggered by any test; the previously-exploited `square_connections` hole has no regression test; `stored_value_ledger` and `credential_references` have no RLS tests; `reconcile_brand_modules`' disable loop is untested. |

---

## 3. Queued — next waves, autonomous

### Security
- **QUEUED** `training-media` is a `public = true` bucket with a tenant-free read policy (`20260826155933:13,28-30`); internal training material is world-readable to anyone who learns a brand id. Make it private; signed URLs; brand-prefix read policy.
- **QUEUED** `config.toml:20` is load-bearing in two directions (blocks direct RPC of unguarded `app.*` definers granted to `authenticated`; makes the five RPCs above dead) and nothing asserts either direction. Add a consistency test that the exposed schema list is exactly `["public","storage"]`.

### App flows
- **QUEUED** Kiosk `stored_value` tender is configured (`brand.json:506`) and module-installed but permanently hidden by `GUEST_LOOKUP_IS_STUBBED = true` (`identify.ts:24-29`); three built screens are dead. Wire a balance-lookup endpoint or surface "configured but unbacked" instead of silently dropping it.
- **QUEUED** Square reauthorization state computed (`square-connection-status-result.ts`) but never rendered — Locations shows "Connected" for a stale OAuth scope contract and offers only Disconnect. Render "Reconnect Square".
- **QUEUED** Operations CSV export gates on legacy `brands.operations` (`export/route.ts:41-45`) while the page gates on `activeModuleKeys` — a new tenant sees the page and a 404 on Export.
- **QUEUED** Profile "Birthday" is dropped by `mobile-api.ts:61-71` while the UI says "Profile saved".
- **QUEUED** Customer home shows no error/retry when the live catalog fails to load.

### Money
- **QUEUED** Stored value is read everywhere on the money path but never written; `nextLedgerBalance` has zero callers. When `stored_value` ships to checkout, a refund of a mixed card+balance order returns the card portion and strands the balance. Add the ledger credit-back before the redemption UI leaves its `isDemo` gate.

### Database
- **QUEUED** `platform_fees.refunded_fee_cents` backfills to 0 with no reconciliation from history — harmless at zero orders; apply `20260912020000` before the first live refund.
- **QUEUED** `docs/RUNBOOK.md` has no rollback procedure for any migration; the chain is forward-only by design. Document how to author a reverting forward migration, with the specific reverts for the three newest.

### CI / observability
- **QUEUED** Android is fully configured in all three native apps and never built in CI (MOB-01). Add an Android export step for customer, operator, kiosk.
- **QUEUED** ~95 `catch` blocks in `apps/hq/lib` log via raw `console.*` with no severity, request id or tenant context. One shared structured logger.
- **QUEUED** The line-length baseline holds 2 grandfathered files (REF-01) — retire them.

### Franchise / tenant
- **QUEUED** `demo.ts` fixtures are still two-way; a third-vertical tenant gets coffee demo orders, "Brew Club", and a coffee reward catalog in demo mode. Write a neutral seed set (the other half of #148).
- **QUEUED** HQ demo console resolves an org unknown to the registry to `DEMO_MODULE_KEYS` — the launch tenant's set — rather than to nothing (`capabilities.ts:59-73`). Demo-mode only.
- **QUEUED** Demo org switcher tags Stillpoint Builders `kind: 'operator'` — a franchisee labelled as the platform operator (`tenants.ts:99-108`). Demo-mode only.
- **QUEUED** `stillpoint-builders` ships no `modifiers.json`, `menu-categories.json`, `packs.json` or `commerce-catalog` config while every sibling and `_template` do; nothing borrows, but the template does not say these are optional for a franchisor-kind tenant.
- **QUEUED** Generic reward-tier fallback says "priority pickup" (`rules.ts:28-33`) — presumes a counter model. Not reachable today; fix before a non-hospitality tenant enables loyalty.
- **QUEUED** `@platform/ui` industry packs cover UI chrome only; marketing prose lives beside the screens. Consolidate once a third vertical's copy exists.

### Tests
- **QUEUED** Menu re-seeding never proves a removed item is gone on re-onboard (`onboard-database-menu.ts:16-44` upserts only). A discontinued item stays orderable.
- **QUEUED** `calendar-training.test.ts:296-304` uses a 5-second wall-clock bound as "was not retried". Replace with an attempt counter.
- **QUEUED** `factory-deployment-producer.test.ts` asserts on call order in source text — deliberate and documented; leave it, but it is the reason `platform-factory.ts` cannot be freely refactored.

---

## 4. Owner actions — cannot be done autonomously

| Item | Why it is yours |
| --- | --- |
| **Apply migrations `20260912000000`–`20260912020000` (and wave 1's) to production with the next deploy, not after it.** `main` pins `20260912020000`; production is at `20260911280000`; HQ deep-health will report the database behind until they land. The database worker ran the full 186-migration chain locally with all 50 assertions passing — they are additive and safe. | Owner-triggered deploy; live database |
| **Reconcile the production brand's active `commerce-delivery` installation row** — `pnpm onboard --tenant coffee-story`. The storefront gate landed; the row is still active. | Live database write |
| **Create `SENTRY_DSN` and `EXPO_PUBLIC_SENTRY_DSN` repository secrets.** Wave 1 wires them; without the values production error tracking stays a no-op. | Secret |
| **Make `hosted-integration` a required check.** It is the only job that runs real migrations, RLS and the cross-surface Playwright suite, and it is gated `!= 'pull_request'` (`verify.yml:230`) — so a PR that breaks a migration or a policy merges green. Requires a PR-safe design for the project-creation token. | Branch protection + secrets design |
| **Add `security-scan` to required checks** once wave 1's workflow has run green on a real PR. | Branch protection |
| **Run a rollback drill (REL-01).** `deploy-hosted.yml` carries ~350 lines of compensation bash that has never executed against a real failure, and the runbook has no post-promotion rollback entry. | A drill |
| **Decide whether `app_fee_money` should exclude tips.** `capture-payment.ts:81` computes the platform fee on `total_cents` including `tip_cents` — 3% of a $5 tip is 15c out of what the guest intended for the barista. Matches rule 3 as literally written; most platforms carve tips out. | Product decision |
| **Kiosk card acceptance.** `CARD_READER_IS_SIMULATED = true` (`card-reader.ts:30`) with no native override — every kiosk offers only "Pay at the counter". Fails safe; needs a real Square Terminal / reader integration. | Hardware integration |
| **`OWNER-ACTIONS.md` sections 2, 4, 5, 7, 8** — repo-fleet unborn HEAD, five unpopped stashes, MCP authorizations, Fly memory ceiling, Fly admission. Pre-existing, unchanged. | Various |
| **Stale worktrees and local branches** — two spent worktrees and eight redundant local refs, all verified as landed on `dev` with remote tips surviving. Removal was denied by the permission classifier; safe to run by hand. | Permission |

---

## 5. Verified clean — do not re-audit

Recorded so the next pass does not repeat the work.

- **RLS coverage:** every `public` table has RLS enabled (148 created, 0 missing); apparent gaps are `app`-schema tables with `revoke all`.
- **Definer hygiene:** all 335 `SECURITY DEFINER` functions pin `search_path` (the `SET search_path TO ''` spelling defeats a naive grep).
- **No policy gated on `jwt_brand_id()` alone**, which is what makes the self-serve `brand_slug` bootstrap claim harmless.
- **Square OAuth tokens** are ciphertext in a table with one service-role policy and appear in no view or definer projection; `resolve_connector_secret` is `service_role` only.
- **Device tokens cannot impersonate staff** (`verifyDeviceToken` rejects `sub` and any `app_metadata.role`); revocation bites on the direct PostgREST path via `app.device_is_active`.
- **`DEMO_SESSION.userId` is `null`** and both `authorizeWorkspaceMutation` and `claimsForWorkspaceMutation` refuse on exactly that, independently — the demo session is structurally incapable of a trusted write.
- **Every service-role path in the console** is closed in demo mode by one of three mechanisms: `serverClient()` null, the null `userId`, or bearer auth.
- **Migration chain:** no duplicate or out-of-order timestamps (enforced by `packages/schema/src/invariants.test.ts`); every `register_release` stamp matches its filename; `pnpm db:local` applies all 186 end to end; `deep-health.test.ts` pins the constant to the newest migration.
- **Hosted promotion** wraps each migration in its own transaction with an idempotency key; a mid-apply network failure cannot double-apply.
- **Money paths:** payment capture, checkout link, webhook settlement and fee quote all cross-validate against durable quotes with exact-match checks and reject rather than guess.
- **a11y-state rule:** every raw `accessibilityState` is paired with its `aria-*` prop; every literal `AppIcon` name resolves.
- **Rule 7:** no operator/admin trees remain in `apps/customer/src` — `docs/AUDIT.md`'s note to the contrary is stale.
- **Display board** reads only the narrow `board_tickets` view — no PII or cart on the public screen.
- **Applied tenant slots** are byte-identical to their `tenants/*` sources for all three applied tenants, both apps.
- **Go-live lock:** nothing was ever minted without approval — the single caller of `mintProductionHosts` sits inside `if (goLiveApproved)`. All five explicit guards were inert; they now carry the real flag and `mintProductionHosts` refuses before its first `await`.

---

## 6. Method notes

- Workers were read-only and non-overlapping; writes are done by isolated wave workers in their own worktrees and integrated by the coordinator.
- Two worker claims were dropped on coordinator re-verification (a "critical" demo-session write path that `userId: null` closes twice; a suspected-untested webhook route that has solid coverage).
- Every fix shipped carries a test shown to fail on the code it fixes. Where a guard was the fix, the failing output is quoted in the PR — a refusal that gets as far as the database is the evidence, not the assertion.
