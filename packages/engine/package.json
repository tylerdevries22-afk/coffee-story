{
  "name": "@platform/engine",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo 'lint: added in Phase 7'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet'",
    "verify": "pnpm typecheck && pnpm test"
  }
}
