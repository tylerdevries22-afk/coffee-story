# Superseded

This guide described the legacy appointments/Stripe plane, which is being
retired. The current production guide is **[PRODUCTION.md](PRODUCTION.md)**:
the platform API on the HQ deployment, Supabase migrations under
`supabase/migrations/`, the auth-hook claim minter, tender-first payments,
and the owner-triggered deploy rules.

Two items from the old gap table that are still live concerns are carried
forward there: the unverified tax table and the unreviewed legal copy.
